import React from "react";

export default function Overview() {
  return (
    <div id="form-grid">
      <input type="text" placeholder="Name (e.g. Altona)"></input>
      <textarea placeholder="Description"></textarea>
      <input type="text" placeholder="Adjective (e.g. Altonian)"></input>
      <select></select>
    </div>
  );
}
